import { UrlPathnamePipe } from './url-pathname.pipe';

describe('UrlPathnamePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlPathnamePipe();
    expect(pipe).toBeTruthy();
  });
});
